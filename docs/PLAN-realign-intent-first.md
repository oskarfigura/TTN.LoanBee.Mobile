# SUPERSEDED — see `docs/PLAN-unified-journeys.md`

This realignment plan has been folded into and replaced by
**`docs/PLAN-unified-journeys.md`**, which keeps the intent-first correction *and* adds
the single start-date-driven mortgage tracking flow (one Track form replaces both
`track.tsx`'s today-anchored path and `history.tsx`).

Do not follow this file. It is kept only as a pointer.
